class Solution:
    def maxSum(self, n):
        if n==0:
            return 0
        dp=[0]*(n+1)
        for i in range(1,n+1):
            dp[i]=max(i,dp[i//2]+dp[i//3]+dp[i//4])
        return dp[n]
        
