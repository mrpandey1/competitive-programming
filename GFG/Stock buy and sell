class Solution:
    #Function to find the days of buying and selling stock for max profit.
	def stockBuySell(self, A, n):
	    ans=[]
	    for i in range(n-1):
	        if A[i]<A[i+1]:
	            ans.append([i,i+1])
	   # print(ans)
	    return ans
        # return ans
