from math import floor, ceil, log
class Solution:
    def isPowerOfFour(self, num: int) -> bool:
        
        if num<=0:
            return False
        x = log(num, 4)
        if ceil(x) == floor(x):
            return True
        else:
            return False
