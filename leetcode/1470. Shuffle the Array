class Solution:
    def shuffle(self, nums: List[int], n: int) -> List[int]:
        result=[]
        i=0
        j=n
        while i<j and n<n*2:
            result.append(nums[i])
            result.append(nums[n])
            i+=1
            n+=1
        return result
            
