class Solution:
    def createTargetArray(self, nums: List[int], index: List[int]) -> List[int]:
        temp=[]
        j=0
        for i in index:
            temp.insert(i,nums[j])
            j+=1
        return temp
            
